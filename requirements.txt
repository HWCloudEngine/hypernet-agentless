# The order of packages is significant, because pip processes them in the order
# of appearance. Changing the order has an impact on the overall integration
# process, which may cause wedges in the gate later.

ryu>=4.10 # Apache-2.0
boto3>=1.4.3 # Apache-2.0

# should already be installed with neutron packages

# pbr>=0.10.0,<1.0
# Babel<=1.3,>=1.3 # BSD
# eventlet<=0.15.2,>=0.13.0
# greenlet<=0.4.2,>=0.4.2
# six<=1.9.0,>=1.7.0
# oslo.config<=1.6.0,>=1.4.0 # Apache-2.0
# oslo.db<1.1,>=1.0.0 # Apache-2.0
# oslo.messaging<1.5.0,>=1.4.1
# oslo.rootwrap<=1.5.0,>=1.3.0
# python-neutronclient>=2.3.8
